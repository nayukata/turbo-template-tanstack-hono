{
  "name": "turbo-template-tanstack-hono",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "check": "turbo run lint check-types",
    "test": "vitest",
    "test:run": "vitest run",
    "format": "biome format --write .",
    "env:encrypt:dev": "dotenvx encrypt -f apps/api/.env.development && dotenvx encrypt -f apps/web/.env.development",
    "env:encrypt:prod": "dotenvx encrypt -f apps/api/.env.production && dotenvx encrypt -f apps/web/.env.production",
    "env:decrypt:dev": "dotenvx decrypt -f apps/api/.env.development && dotenvx decrypt -f apps/web/.env.development",
    "env:decrypt:prod": "dotenvx decrypt -f apps/api/.env.production && dotenvx decrypt -f apps/web/.env.production",
    "db:generate": "turbo run db:generate --filter=@repo/db",
    "db:migrate": "turbo run db:migrate --filter=@repo/db",
    "db:push": "turbo run db:push --filter=@repo/db",
    "generate:api-types": "turbo run generate:api-types"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@dotenvx/dotenvx": "^1.51.4",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "turbo": "^2.7.2",
    "typescript": "catalog:",
    "vitest": "^4.0.16"
  },
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.25.0"
    }
  }
}
